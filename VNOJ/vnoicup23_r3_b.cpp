#include<bits/stdc++.h> 
using namespace std;
int n, a, b, 
bool check(int l , int r) {
    if(f[l][r] != -1) return f[l][r];
    if(l <= b - a || l >= b) return true;
    

}
int main() {
    cin >> t;
    while(t--) {
        int n , a , b , tmp = 0;
        string s;
        cin >> n >> a >> b >> s;
        for(char c : s) {
            if(c == '0') tmp++;
        }
        cout << Check
    }
}